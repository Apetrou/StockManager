{% extends 'base.html.twig' %}

{% block title %}Customer index{% endblock %}

{% block body %}
    <h1>Customers
        <span class="float-right"><a class="btn btn-success" href="{{ path('customer_new') }}"><i class="fas fa-plus"></i> Create new</a></span>
    </h1>

    <table class="table table-striped table-bordered" id="customerTable">
        <thead>
            <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Telephone Number</th>
                <th>Shipping Method</th>
                <th>Comments</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for customer in customers %}
            <tr>
                <td>{{ customer.firstName }}</td>
                <td>{{ customer.lastName }}</td>
                <td>{{ customer.telephoneNumber }}</td>
                <td>{{ customer.shippingMethod }}</td>
                <td>{{ customer.comments }}</td>
                <td class="text-center">
                    <a class="btn btn-secondary" href="{{ path('customer_show', {'id': customer.id}) }}">show</a>
                    <a class="btn btn-warning" href="{{ path('customer_edit', {'id': customer.id}) }}">edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}