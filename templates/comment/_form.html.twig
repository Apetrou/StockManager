{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-default float-right">{{ button_label|default('Send') }}</button>
{{ form_end(form) }}
<div class="clearfix" style="margin-bottom: 70px"></div>